export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-surface-border bg-white">
      <div className="container-page flex flex-col gap-2 py-8 text-sm text-squid/60 sm:flex-row sm:items-center sm:justify-between">
        <p>AWS UET Taxila — Builder performance record.</p>
        <p>Scores are recorded with a written justification and an audit trail.</p>
      </div>
    </footer>
  )
}
